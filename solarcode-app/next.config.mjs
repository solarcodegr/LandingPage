/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '', // Leave empty if deploying at root.
    assetPrefix: '', // Replace <repo-name> with your GitHub repository name
};

export default nextConfig;
